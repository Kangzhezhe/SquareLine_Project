{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",//编译的项目名，build
            "type": "shell",
            "command": "./make.bat",//编译命令
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "make clean",
            
 
        }
    ]
}